name: First CI Workflow

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show Greeting
        run: echo "Hello Rinku! Workflow is working 🎉"
